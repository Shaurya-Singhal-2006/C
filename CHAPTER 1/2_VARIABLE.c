//                                                          VARIABLE 

// variable is the name of a memory location which stores some data 

a = 25;               
// 25 and 45 values are stored in variables (a,b)(respectively)    
b = 45; 

// it can also store character 
c = ("shaurya"); 


// FOR EXAMPLE 


#include <stdio.h>

int main(){

 int number = 78;                        // variabls are case sensitive (a,A)(both are different variable)   
 char star = "*";                        // 1st character is 'alphabet' or '_' (for eg. age = 18 or _age = 18)
 int age = 18;                           // no commas / blank space  (final price [WRONG] , final_price [CORRECT])
 float pi = 3.14;                        // no other symbol than '_'

 return 0; 
}