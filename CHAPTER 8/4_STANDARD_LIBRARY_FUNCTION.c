//                                 STANDARD LIBRARY FUNCTIONS (string.h)

// 1. (strlen) --> count number of characters excluding '\0'
// 2. (strcpy) --> copies the value of old string to new string 

/*
char str1[] = "hello";
char str2[] = "world";

strcpy(str1,str2);
now str1 = world;

*/

// 3. strcat(firststr,secondstr) --> concatenates first string with second string
//size of first string should be bigger that size of first string + size of second string . (else it will show error)

// 4. strcmp(firststr.secondstr) --> comapre 2 string and returns a value 

/* outputs
 A.  0 --> equal string
 B. positive --> first > second (ASCII)
 C. negative --> first < second (ASCII)
*/


//EXTRA INFO

//SALTING --> companies add random charachter in the our password so the hacker can not hack it easily