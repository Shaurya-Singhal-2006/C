// realloc()

//reallocate (increase or decrease) memory using same pointer and size

// eg   
//ptr = realloc( ptr  , new size);
//                ^
//    name of the pointer you want to change the size of 